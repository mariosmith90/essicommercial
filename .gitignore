# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
functions/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# build artifacts written to public/ by scripts/copy-build.js
/public/_next/
/public/index.html
/public/404.html

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Service account keys — never commit
*-key.json

# env files
.env*
# functions/.env only has non-sensitive values (client email, bucket name)
# ESSI_GCS_PRIVATE_KEY is stored in Firebase Secret Manager
!functions/.env

# firebase
.firebase/

# GCP service account keys (never commit)
firebase-service-account.json
gcs-service-account.json
*.json.key
.pnpm-debug.log*

# Terraform state (local backend — never commit)
infra/terraform.tfstate
infra/terraform.tfstate.*.backup
infra/.terraform/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
